version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci  # Using ci instead of install for more reliable builds
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - ~/.npm