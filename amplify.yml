version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - cd compute && npm ci && cd ..
    build:
      commands:
        - bash build.sh
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
